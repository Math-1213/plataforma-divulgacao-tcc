# Dockerfile para Desenvolvimento - Backend (AdonisJS)
# Este Dockerfile é usado apenas para desenvolvimento com hot-reload

FROM node:20-alpine

WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./
COPY yarn.lock* ./

# Instalar todas as dependências (incluindo devDependencies)
RUN npm ci

# Expor porta
EXPOSE 3333

# O comando será sobrescrito no docker-compose.dev.yml
CMD ["npm", "run", "dev"]

