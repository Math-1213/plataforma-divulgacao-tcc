# Dockerfile para Desenvolvimento - Frontend (React)
# Este Dockerfile é usado apenas para desenvolvimento com hot-reload

FROM node:20-alpine

WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./

# Instalar todas as dependências
RUN npm ci

# Expor porta
EXPOSE 3000

# O comando será sobrescrito no docker-compose.dev.yml
CMD ["npm", "start"]

